require 'spec_helper'

describe Frest::Composers do
  it 'has a version number' do
    expect(Frest::Composers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
